# actionlint's built-in hosted-runner list predates GitHub's xcode-27 preview.
# This label allowance does not change the workflow's GitHub-hosted execution.
self-hosted-runner:
  labels:
    - xcode-27
